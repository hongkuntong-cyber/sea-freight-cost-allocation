# 后端服务配置示例（复制为 .env 后按需修改；正式环境务必收紧 CORS）
# 数据库文件位置（绝对路径或相对路径）
SEA_FREIGHT_DB=./backend/data/sea_freight.db

# 跨域白名单：开发可用 *；正式集成 ERP 时改为 ERP 域名，例如
# CORS_ORIGINS=https://erp.your-company.com
CORS_ORIGINS=*

# 服务监听端口
PORT=8000
